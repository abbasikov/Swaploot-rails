<div class='text-sm px-4 py-2  w-fit mt-4'>
  <%= form_tag(errors_path, method: :get) do %>
    <div class='flex'>
      <div class='text-blue dark:text-white'>
        Time: <%= select_tag :time_range, options_for_select(filters_by_time, params[:time_range] || 'All'), class: 'border rounded px-2 py-1 w-28 text-grayDark', onchange: 'this.form.submit();' %>
      </div>
      <div class='bg-gray w-px mx-4'></div>
      <div class='text-blue dark:text-white'>
        Source: <%= select_tag :error_source, options_for_select(filters_by_error_source, params[:error_source] || 'All'), class: 'border rounded px-2 py-1 w-28 text-grayDark', onchange: 'this.form.submit();' %>
      </div>
    </div>
  <% end %>
</div>
